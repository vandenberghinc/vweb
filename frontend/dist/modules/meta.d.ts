export declare const Meta: {
    set({ author, title, description, image, favicon, }: {
        author?: string;
        title?: string;
        description?: string;
        image?: string;
        favicon?: string;
    }): void;
};
